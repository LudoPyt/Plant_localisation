<%= form_with(model: position, local: true) do |form| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= simple_form_for @position do |f| %>
    <%= f.input :latitude, label: 'Latitude', id: "latitude"%>
    <%= f.input :longitude, label: 'Longitude', id: "longitude" %>
    <div id="alert-localisation"></div>
    <%= f.collection_select(:plant_id, Plant.all, :id, :name) %>
    <%= f.button :submit %>
  <% end %>

    <button id="btn-loc">Localiser position</button>
<% end %>
